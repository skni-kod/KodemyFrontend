import React from 'react';

import SectionBox from '../SectionBox';

import { TEXT } from '@/utils/constant';

export default function AddTagsSection() {
	return <SectionBox title={TEXT.DEV_TOOLS.ADD_TAGS}>dodawanie tagów</SectionBox>;
}
